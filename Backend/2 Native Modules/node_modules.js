/*
native node modules
see steps what you want to do from nodejs.org docs
*/

const fs = require("fs"); //shortened for file system
fs.writeFile("./msg.txt","Hello from node js",(err)=>{
    if(err) throw err;
    console.log("The file has been saved");
});
//Mentioned no path so file will be created in parent folder itself
//next parameter is string which tells what to write and next is callback function

fs.readFile("./NodeJs/text.txt","utf8",(err,data)=>{ //file address , encoding , function
    if(err) throw err;
    console.log(data);
});